import { FormatPopulationPipe } from './format-population.pipe';

describe('FormatPopulationPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPopulationPipe();
    expect(pipe).toBeTruthy();
  });
});
